This folder contains reference implementations of the the reinforcement learning algorithm in Python (first implem) and C (manual port). The later was used as a base before actor splitting in Preesm.
